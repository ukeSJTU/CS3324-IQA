Save all necessary figrues and tables in this `assets` folder.

The `reference.jpg` is the image given by TA to evaluate the computational complexity analysis of the model. Compute the FLOPs and throughput time of the model on this image, and report the results in the final report.